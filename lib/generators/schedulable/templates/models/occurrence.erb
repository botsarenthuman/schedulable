class <%= name.classify %> < ActiveRecord::Base
  belongs_to :schedulable, polymorphic: true
  default_scope lambda{order('start_time ASC')}
  scope :remaining, lambda{where(["start_time >= ?",Time.now])}
  scope :previous, lambda{where(["start_time < ?",Time.now])}
end
